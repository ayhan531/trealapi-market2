// Collector has been intentionally disabled.
// Keeping the signature so other imports do not break.
export async function startTradingviewApiCollector() {
  console.log('[TV-API] Kolektor devre disi birakildi. Veri cekme islemleri calismayacak.');
  return () => {};
}
